# P0 / P1 / P2 検証記録

2026-09-16、macOS arm64 / Node 22.17.1 / npm 10.9.2。

## 自動検証

- P0は `npm ci` のクリーン導入から `npm run check` を実行。C majorの7三和音・Bdimの構成音、本番ビルドのroot/subpathで音声開始・切替・停止を確認して `fd86d9d` に記録。
- P1は `npm run check`（strict型検査、ESLint、Prettier、26 unit tests、production build）。楽理・配置・session・時計・Pages baseを検証。
- Playwright Chromium: 8シナリオ × desktop/mobile = 16 testsを `/` と `/chordscape-smoke/` の各production buildで実行。
- JS/CSSの読込失敗・page error・画面の横方向overflowを検出。desktop/mobileの全画面画像も目視確認。
- `npm run dev -- --port 4175 --strictPort` をChromiumで開き、タイトル表示・page errorなしを確認。
- `git diff --check`、lockfileとenginesの整合性、Pages Actionsの公式release SHAを確認。

## 重点ケース

楽理: C major / F major / F♯ major / A minor、C7とCmaj7、Bm7♭5とBdim7、C/E・C/G・G7/F（V4/2）、Am/Gを三和音の転回として拒否、短調のセカンダリードミナントの♭付き解決先、全調＋調外候補の移調・JSON往復。

配置・保存: 手動bass優先、三和音↔四和音の挿入／削除コスト、ループ境界、決定的な再計算、長さ変更だけでは実音配置を維持、調選択は既存音高を維持、全体移調は各文脈も移動、stable ID、Undo/Redo、JSON不正入力・件数上限・矛盾したnotes/bassを拒否、storage禁止時にも試聴・書出し可能。

再生: 注入した時計でStop・Pause/Resume・テンポ変更・周回編集・短いループ・遅延した時計を検証。Chromiumでは実際のAudioContextと出力波形を使い、発音時に信号があること、Stop後に無音になること、中断時にPauseしてユーザー操作で復帰することを検査。mockのみの確認ではありません。一時停止後の全消去で旧進行を再開しないことも確認。

## 未実施・制約

- 人による試聴は未実施。Root/Smoothの自然さ、click、音量差、低域の濁りについて品質保証はしません。
- 実機Chrome / Firefox / Safari・iOSでの音声開始・中断・復帰、端末固有の音量や背景制限は手動確認事項。
- GitHub上のworkflow実行・Pagesへのデプロイは未実施（pushしていません）。Pages SourceはAPIで `build_type: workflow` に設定済み。公開範囲は変更していません。
- P3–P5は未実装。P2のseedと生成設定はschemaVersion 2へ追加しました。独立した転調イベントはP3で拡張します。

## モバイル画面・音色の改修（2026-09-16）

- 演奏／辞典／設定をhashによる別画面に分割。下部ナビゲーション、ブラウザの戻る、移動後の見出しへのフォーカス、横方向overflowなしを検査。
- モバイル幅320 / 375 / 390pxでダイアトニック7コードが同じ行にあり、鍵盤と進行見出しが高さ812pxの初期画面内にあることを検査。画面画像も目視確認。
- `npm run check`: 28 unit tests。音色の保存・既存sessionの移行・不正値の拒否、同梱サンプルと原文READMEのSHA-256／サイズ一致を追加。
- Playwright: 12シナリオ × desktop/mobile = 24 testsを `/` と `/chordscape-smoke/` の各production buildで実行し、すべて成功。4音色の実際の発音と停止、音色変更による停止、17サンプルのbase付きURL、Piano読込失敗時の代替音色と再試行、遅れて発音しないことを追加。
- Electric / Pad / Softの初回発音は出力波形のピーク、隣接サンプルの差、DC成分、35 Hz未満のスペクトルを検査。発音予約直後のStopでも音と鍵盤表示が残らないことを検査。
- 停止時の既存envelopeの書換えで波形が飛ぶケースをOfflineAudioContextで再現し、独立した停止用gainで18msのfadeを行うよう修正。実際のvoice実装に対し、3音色 × 7停止時点（発音前・attack・decay・release）の波形連続性と停止後の無音を決定的な時計で検査。

### 低域ノイズの調査

旧音源は正弦波ではなくTone.PolySynthのtriangle。ユーザーが報告した強い低周波ノイズはChromiumでは再現できず、原因を断定していません。

ChromiumのOfflineAudioContext（48 kHz）でC3 / E3 / G3を1秒鳴らし、旧音源と新4音色を比較しました。前半0.5秒にHann窓をかけたFFTの35 Hz未満のエネルギー比と、全1.2秒のピークを記録しています。新音源は35 Hz high-passと初期音量0.5を含みます。端末のスピーカー特性を含む測定ではありません。

| 音色                 | 波形ピーク | 35 Hz未満のエネルギー比 |
| -------------------- | ---------: | ----------------------: |
| 旧Soft synth         |      0.131 |               0.000158% |
| Electric piano       |      0.181 |               0.000005% |
| Pad                  |      0.160 |               0.000001% |
| Soft synth（改修後） |      0.152 |               0.000002% |
| Piano                |      0.115 |               0.049165% |

発音開始前の信号はゼロ、波形の飽和はありません。合成音源の超低域成分は減っていますが、これだけで報告された症状の解消や聴感の良さを保証できません。端末・ブラウザ・出力機器を含めた実機試聴は未実施です。

## 構成上の判断

初回から既存の `7298218 Initial commit` があったため履歴を保持しました。P0、楽理・配置、編集・時計、P1画面統合を別コミットにしています。

Loopの次周回は120ms先までの予約時点で固定します。編集中の新データと発音中のsnapshotを分け、Chord detailsは発音中のイベントを優先表示します。Pause中の進行編集は明示的にStopへ戻し、古いsnapshotが再開されるのを防ぎます。

## P2 / Auto（2026-09-16）

モバイル・音色の改修を `npm run check` で再検証して `3796969` にコミットしてから着手しました。

- `npm run check`: strict型検査、ESLint、Prettier、38 unit tests、production build。
- 生成の設定の組合せ（Pop/Jazz、4/8/16小節、2/4/8拍間隔、三和音/七の和音、終止/ループ）について、拍数・終端・構成音・生成意図を検査。
- 全30調で調外0%/100%のJazz生成、Smooth配置、JSON往復、半音移調後の綴り・意図・音高を検査。同じseed・設定・version・フレーズ番号の再現性と、seedによる変化を検査。
- C major / Jazz / 4小節 / 4拍 / seventh / outside 100% / loop / seed 42でCmaj7 → A7 → Dm7 → G7、Imaj7 → V7/ii → ii7 → V7を検証。並べ替えでA7の後がDm7でなくなると、現在の解決についての説明が変わることを検査。
- Cadd9 / C9 / Cmaj9 / Cm6、9thをbassにしたC9/D、5音を省略しないこと、拡張和音の転回数字がundefinedにならないことを検査。
- schemaVersion 1から2への移行で確定音高を保持。不正seed・version・phrase・矛盾した生成意図を拒否。未編集の生成結果とseedの不一致も拒否。
- 連続生成は少数のフレーズを事前生成。偽の時計で境界直前のPause/Resume・テンポ変更による再予約が同じフレーズ番号を使うこと、Stopで準備済みデータを無効にすること、自動切替がUndo履歴を増やさないことを検査。
- 次フレーズの先頭を予約前から正しく表示し、切替後も前の和音を保持して比較することを検査。PopのI–V–vi–IVを終端処理で変更せず生成できることも検査。
- PlaywrightはP2の5シナリオを追加し、全17シナリオ × desktop/mobile = 34 testsを `/` と `/chordscape-smoke/` の各production buildで実行し、すべて成功。生成→編集→再生成→Undo/Redo、発音・停止、JSON書出し/読込/reload、辞典の5音同時発音、連続生成の境界・Pause/Resume・途中編集での停止、fallback表示、320px幅の生成画面を検査。
- 日本語の生成画面・生成後の演奏画面をdesktop/mobileの画像で目視確認。

実機での試聴、フレーズの自然さの聴感評価、Safari/iOSでの手動確認は未実施です。追加style、tritone substitution、P3–P5は提供していません。

## P3 / Circle（2026-09-16）

P2を `npm run check` で再検証し、`30bfff0` にコミットしてから着手しました。

- `npm run check`: strict型検査、ESLint、Prettier、47 unit tests、production buildが成功。
- C / Gの共通三和音C・Em・G・Amと、共通七の和音Cmaj7・Em7・Am7を検査。qualityと全構成音の一致を条件にし、同じ根音だけでは共通和音と扱わないことを確認。
- C → Am → D7 → Gの旧新分析、Gでの調の切替、任意の新調cadence、遠隔調での直接転調、同調・異名同音の同調への転調を提案しないことを検査。
- F♯ → D♭の共通和音は旧調でD♯m（vi）、新調でE♭m（ii）として綴り分けます。実際に鳴らすpitch classを変えず、それぞれの文脈で分析することを検査。
- 全30調・両方向の12区間巡回、出発調への帰着、三和音／七の和音、Smoothの構成音を検査。巡回のJSON往復と、-1 / +1 / +7半音の全体移調後に旧新の転調意図・調の切替・確定音高が整合することを確認。
- KeyEventは安定eventIdに結び付け、拍位置を順序と長さから導出。長さ変更・並べ替え・削除・Undo/Redoで境界が追随することを検査。参照先のない境界・誤った拍位置・矛盾した調・偽のピボットをJSON読込時に拒否。schemaVersion 1 / 2から3への移行で確定音高を保持。
- 偽のaudio clockで、Gが先読み予約されてもD7の間は旧調を保つこと、境界直前のPause/Resume・テンポ変更、次周回のsnapshot、Stopでの予約破棄を検査。
- PlaywrightのP3シナリオは、比較とパレット変更の区別、SVGの矢印キー／フォーカス、異名同音表示、追加前の旧新分析、Gの発音とパレットの同時切替、Pause/Resume・Stop・再生し直し、試聴後の保存データ保持、巡回・移調・JSON再読込・Undo、直接転調・cadence・七の和音比較を含みます。短い試聴のC→G→C表示はブラウザ内のMutationObserverで記録し、テストのポーリング間隔に依存させません。
- `/` と `/chordscape-smoke/` のproduction buildで、各22シナリオ × desktop/mobile = 44 tests、合計88 testsが成功。既存P1/P2の再生、保存、生成、4音色、停止時の無音も含みます。
- desktop（1280px）、mobile（390px）、最小幅（320px）の五度圏画面を撮影し、desktop/mobile画像を目視確認。横overflowがないことと、演奏画面の7和音が横1列に収まることをブラウザで確認。

実機での試聴、転調の自然さの聴感評価、Safari/iOSでの手動確認は未実施です。P4の旋律、P5の読み上げ・quiz、追加styleやオフライン起動は未実装です。

## P4 / Melody（2026-09-16）

P3を `npm run check` で再検証し、`ac4620f` にコミットしてから着手しました。

- `npm run check`: strict型検査、ESLint、Prettier、60 unit tests、production buildが成功。
- 旋律generator version 1・旋律Seed・モチーフSeed・設定・進行から同じ音符列を再現。旋律再生成が伴奏のコード・確定音高・調・長さを変更しないこと、モチーフ保持とUndoを検査。
- 音域48–96、最低1オクターブ、3種類の音符間隔、0.03123 / 0.25 / 1 / 4 / 16拍の長さについて、音符の範囲・綴り・時間の被覆・強拍の和声音を検査。A7のC♯など調外和音の構成音を使うことを確認。
- 経過音と次の和音へのアプローチは実現した前後関係を検査して分類。休符が解釈を遮ること、Liveでは将来の和音へのアプローチを約束しないことを検査。
- 全30調からのブリッジに旋律を生成し、JSON往復、-1 / +1半音移調後の音高・綴り・分類が整合することを確認。schemaVersion 3から4の移行は旋律オフを初期値とし、既存の確定音高を保持。不正な音符・時間・Seed・根拠のない解釈を拒否。
- 偽のaudio clockでTimelineの両partの同時予約、Pauseでの予約取消、Resumeでの残りの旋律音の再発音、Stop後の無音を検査。
- LiveのNext beatで、既に先読み予約した候補を境界直前に上書きできること、境界前の表示は旧和音であること、両partが同じ境界で最新の和音へ切り替わることを検査。
- Immediateの実際の適用拍と短い継続時間（0.463 / 0.117拍など）を丸めず記録。将来の未適用の候補は記録せず、Liveの反復フレーズから実際の記録区間を切り出すことを検査。Pause/Resume・テンポ変更・長いclock stall・記録上限も検査。
- 実際のvoice実装をChromiumのOfflineAudioContextで実行し、将来の切替時刻に予約したreleaseを、それより早いStopで取り消せること、発音前に停止した古い旋律が無音になることを確認。既存のattack / decay / release途中の停止波形検査も維持。
- Playwrightに6シナリオを追加。旋律の保存・再生成・Undo・JSON書出し/読込/reload、両partの再生・Pause/Resume・Stop、Liveの予約上書き・実測拍の記録・Timelineへの排他的な切替、旋律オフ、320pxの設定画面、伴奏編集後の再計算、P2連続生成の次フレーズ準備を検査。
- `/` と `/chordscape-smoke/` のproduction buildで各28シナリオ × desktop/mobile = 56 tests、合計112 testsが成功。
- desktop（1280px）とmobile（390px）の旋律設定・ピアノロール画面を撮影し、mobile画像を目視確認。320pxで横overflowがないこと、旋律オフ時の演奏画面で7和音・鍵盤・進行が収まる既存検査も成功。

初期状態では旋律オフです。旋律はSoft synth、4/4、単旋律、4拍／8拍のモチーフに限定します。五線譜、音符を直接編集する機能、P5の読み上げ・quizは未実装です。実機での試聴、旋律の自然さや伴奏との音量バランスの聴感評価、Safari/iOSでの手動確認は未実施です。

## 操作パネル・モバイル表示の調整（2026-09-17）

- 配置・A/B試聴・全体移調を進行パネル上部へ常時表示し、折りたたみとその説明文を削除。
- モバイルの調・音階・和音種・テンポ等を高さ86pxの2段に集約。入力の文字サイズは16px、ダイアトニック7和音は横1列を維持。
- 調外和音は高さ44pxのボタンとし、320px幅で6列、390px幅で8列。下部ナビゲーションは320 / 375 / 390pxで文字が1行に収まり、横overflowがないことを確認。
- 初期音色と音色未指定の旧JSONの補完をPianoに変更。明示的に保存された音色は保持し、Piano読込失敗時はElectric pianoで演奏・再試行できることを検査。
- `npm run check`（型・lint・format・60 unit tests・production build）が成功。ブラウザ検証は通常URLで56 tests、Pages相当の `/chordscape-smoke/` で主要操作・4音色・停止・保存等の24 testsが成功。
- 320 / 390pxの演奏画面・調外和音のスクリーンショットを目視確認。実機およびSafari/iOSの手動確認は未実施。

### 1行の調パネル・調外候補の拡充（2026-09-17）

- モバイルの調・音階・和音種を高さ38pxの1行に変更。テンポ・拍数・記録・ループは進行パネルへ移動し、再生・配置比較のボタンは高さ28pxに縮小。短い試聴ラベルでも読み上げ名は維持。
- 調外候補を長調28／短調25に拡充。三和音・属九のセカンダリードミナント、同主調の七の和音、短調の導音上の減七を追加。調内だけの和音と重複は除外。
- 全30調で候補の重複・変化音・配置の構成音を検査。A9のC♯、G♯dim7のFなど綴りと分析も検査し、61 unit testsを含む `npm run check` が成功。
- ブラウザで追加候補の選択・発音・構成音表示を確認。既存の保存・移調・連続生成・旋律・停止を含む54 testsが成功。長いローマ数字でボタン高が増える問題を修正し、該当するdesktop/mobileの表示2 testsも再実行して成功。
- 320 / 375 / 390pxで調パネルと再生ボタンが横1行に収まることを検査。320 / 390pxの画面を目視し、英語の320px表示も横overflowがないことを確認。Safari/iOS実機での手動検証は未実施。

## P5 / 事前生成する聞き流し（2026-09-17）

コード辞典から独立した聞き流しsceneへ遷移。根音固定、調の感覚、完全ランダム、調内・音だけの4モードを実装しました。既定20分の音声を事前に作り、ネイティブaudioで同じセットを繰り返します。

- `npm run check`: strict型検査、ESLint、Prettier、71 unit tests、production buildが成功。
- 全30調の7和音、主和音の基準提示、12音の独立抽選、同じ和音の連続、17種類のquality、根音固定、転回時の実音・最低音、Seed再現を検査。
- 発音用語はコード記号と分離。短調の♭III、E♯、半減七、転回形の名前を検査。音声素材について発音辞書・SHA-256・出典表記を照合。
- 20分のフレーム長、名前と音の順序、回答後の再試聴、末尾で名前や音を切らないこと、シーク／周回境界のcue判定を検査。時刻を秒へ変換して戻す際の浮動小数点誤差はサンプル単位の丸めで処理。
- native audioのcurrentTimeを字幕・コード・鍵盤の基準にし、visibilitychangeで停止しないこと、Media SessionのPause／Stop／Seek、遅いplay完了がStop後に再開しないこと、dispose時の解放を検査。
- 実際に20分・22,050 Hz・mono PCM16のWAVを作り、duration=1200秒、非ゼロの音声波形、ヘッダーの長さ、ダウンロードの著作者・CC BY 3.0メタデータを確認。
- 名前なしモードはspeechアセットを取得せず、native loopで先頭へ戻ること、反復オフでは末尾で終了することを確認。作成キャンセル・音声取得失敗・作成中のscene移動が遅延再生を起こさないことを確認。
- 通常URLで全32シナリオ × desktop/mobile = 64 testsが成功。`/chordscape-smoke/` では新しい聞き流しの4シナリオ × desktop/mobile = 8 testsが成功。
- 390pxの作成画面と再生画面を撮影して目視。320pxでも横overflowなし。作成後はプレーヤーを上部へ表示して見出しへフォーカス。この開発端末のChromiumでは20分セットの作成が約1.4秒でした（実機の性能を保証する測定ではありません）。

音色はPiano、名前音声は日本語のVOICEVOX Nemo 女声1に限定しています。5 / 10 / 20分のセット長と反復オン／オフを提供し、別の独立したスリープタイマー、英語音声、声の切替、quiz、画面オフ中の無限新規生成は未実装です。ブラウザ再読込後は設定を復元して再生成します。生成WAVを自動で永続保存する機能はありません。

**iPhone / Android実機での画面ロック中の長時間再生、OSのイヤホン操作、通話割込み・復帰、Safariでの手動試聴は未実施です。** ブラウザエミュレーションの成功を実機検証の代替としていません。

### 読み上げ音声の変更：Nemo 女声1（2026-09-17）

- 試聴で選ばれたVOICEVOX Nemo 女声1（ノーマル、style 10005、engine 0.24.0）を採用。コード名と調の案内を一続きの音声にし、度数・転回形も同じ声で生成。737 MP3、計11,618,477 bytesを同梱し、旧Mei素材を聞き流しから除去しました。試聴ページの比較用Mei音声は保持しています。
- 受理する全根音・調の綴り、17 quality、度数・転回形の音声が辞書に存在し、再生候補の全プールをカバーすることを検査。必要なプールだけ取得し、既定の根音固定では5ファイル、音だけモードでは0ファイルであることを確認。
- 全音声のフレーズ・SHA-256・MP3内クレジット・話者IDを検査。アプリ画面と書出しWAVにもVOICEVOX Nemoを記載し、PianoのCC BY 3.0とは利用条件を分離。
- `npm run check` が成功（strict型検査、lint、format、72 unit tests、production build）。ブラウザでは通常URL 8 tests、`/chordscape-smoke/` 8 testsが成功（各4シナリオ × desktop/mobile）。
- 実際の20分WAV、再生・停止・シーク・反復、名前を後から提示する順序、転回形と半減七、準備中止・欠損音声・scene移動、Nemoの保存時クレジットを確認。Cmaj7のMP3について、ブラウザでのデコード・非ゼロ波形・クリッピングなしも確認しました。
- 実機の画面オフ中の長時間再生、Safari/iOSの手動検証は引き続き未実施です。

### force push後の修復（2026-09-17）

- 旧mainの `ea6efcc`（add license）からルートのMIT `LICENSE`を復元。Git blob SHA `39edce81de44588f14068d737fb585f691917c7e` が元ファイルと一致し、READMEでソースコードと音声素材の利用条件を区別しました。
- 旋律E2Eの保存待ちが、保存前の `events: []` に対して `events[0].melody` を読んで例外になる問題を修正。保存済みデータの取得をnullableとして扱い、4和音すべての旋律が保存されるまでpollします。固定時間の待機やアプリ側の保存間隔変更は不要です。
- 英語フッターをP5の聞き流し実装に合わせて更新しました。
- `public/speech` は737 MP3と出典・利用条件・manifestの3ファイル、計740ファイルすべて追跡済み。`git ls-files -z` と実ファイルを照合し、未追跡・欠落・余剰音声は0件。全音声のSHA-256がmanifestに一致するため削除していません。
- `npm run check` が成功（型・lint・format・72 unit tests・build）。`CI=1` の設定で全ブラウザテストが `/` 64件、公開先と同じ `/chordscape/` 64件、計128件成功。GitHub Actions上の確認は修復コミットのpush後に行います。

### ダイアトニック二行表示・辞典の17ボタン（2026-09-17）

- 演奏画面の三和音／七の和音の選択をなくし、各7和音を二行で常時表示。五度圏の比較・移動に使う和音種の選択は五度圏内へ移動しました。
- 辞典は根音に対応する17種類をボタンで一覧表示。音声開始前は選択・構成音の確認、開始後は直接試聴でき、進行への追加は専用ボタンで行います。
- `npm run check` が成功（型・lint・format・72 unit tests・build）。通常URLの初回全体検証は64件成功、旧レイアウトのセレクターに依存する2件を修正後、関連6件を再実行して成功。公開先と同じ `/chordscape/` では全66件が成功しました。
- 320 / 375 / 390pxで各行7和音、調パネルの1行表示、ナビゲーションの折り返し防止を検査。辞典の17種類の試聴、根音変更、キーボード選択、試聴で進行が変わらないことを確認。390pxの演奏画面と320pxの辞典を目視確認しました。
- Safari/iOSおよびスマートフォン実機の手動検証は未実施です。

### 任意の和音を調外パレットへ追加（2026-09-17）

- 「＋ 追加」からモーダルを開き、根音と17種類のqualityから和音を登録。既定候補を維持し、追加分の個別削除と同じ綴り・qualityの重複防止に対応しました。
- 追加分は進行とは独立してブラウザへ即時保存。リロード・scene移動で復元し、調を変えても根音とqualityを保持します。進行のJSONには含めません。保存禁止・不正な保存データでも既定パレットを利用でき、保存失敗時には追加分をメモリ上で保持します。
- `npm run check` が成功（型・lint・format・72 unit tests・build）。新規パレットと既存演奏画面のブラウザ検証32件がdesktop/mobileで成功しました。
- 登録・発音・鍵盤とコード名・復元・削除・重複・保存失敗・不正データ、Enterでの選択、Escapeで閉じた後のフォーカス復帰を検査。320 / 390pxでモーダルの横overflowがなく、320pxの画像を目視確認しました。
- Safari/iOSおよびスマートフォン実機の手動検証は未実施です。

### 辞典の33種類への拡張（2026-09-17）

- 7♭5、m(maj7)、7sus4、7♯5、maj7♯5、6/9、m(add9)、7♭9、7♯9、maj7(♯11)、11、m11、13、m13、maj13、7(♭9,♯5)を追加。辞典と追加パレットは全33種類を表示します。
- 11系は6音、13系は7音を省略せず再生。第5・第6転回の表示を追加し、ローマ数字でも変化音・拡張音を区別します。
- 追加16種類の構成音の綴りを検査し、辞典の全根音・全転回形で基本配置・smooth配置・最低音・JSON往復を検査しました。
- 聞き流しは録音済みの既存17種類を明示的に選別。未収録の新規qualityを保存設定からも受理せず、既存737音声の整合性検査を維持しています。
- `npm run check` が成功（型・lint・format・75 unit tests・build）。PC・モバイルの全ブラウザテスト74件が成功。全33ボタンの試聴・第6転回のリロード後の復元・追加パレット・既存の生成と聞き流しを確認しました。
- 320pxの辞典画面を撮影して目視確認。Safari/iOS・スマートフォン実機での試聴、および追加16種類の名前音声は未対応です。

### 指定順の既定調外パレット（2026-09-17）

- C major・C natural minorの指定リストを基準に、全調へ根音の度数と綴りを保って移調。カテゴリ名を付けず各27ボタンを表示します。D♭/F相当は省略し、短調のF7相当は指定の2か所に表示します。
- 既定候補の意図的な重複を保ちつつ、ユーザー追加分との重複は表示しません。追加済み判定・削除・保存の動作を維持しました。
- パレットの異名同音候補が転調ブリッジの属和音を誤った綴りへ置き換えないよう、転調先から導いた綴りを保持。C→F♯のC♯7がD♭7に変わらないことを既存テストで検査しています。
- `npm run check` 成功（75 unit testsを含む）。全30調で順序・quality・度数・pitch class・構成音を検査し、C majorとC minorの全リストの一致を確認しました。演奏・追加パレット・五度圏のPC／モバイル46 testsが成功。ブラウザでC major・D major・C minorの全表示順も確認しています。

### コード辞典の学習順・6列表示（2026-09-17）

- 作業開始前の33種類への拡張と既定調外パレット変更を `704f7f6` にコミットしました。
- 辞典をBasic、Seventh、Added tones / Sixth、Extensions、Alteredの5カテゴリに分け、小さな見出しと6列で表示。指定された33種類の順序を固定し、根音の変更後も保持します。Extensionsは6個＋3個の2行です。
- 長いコード名は括弧・変化音の前で折り返せるようにし、320px幅でもボタン内に収まることを確認しました。
- `npm run check` 成功（75 unit testsを含む）。辞典のPC／モバイル6 testsが成功し、33種類の表示順・見出し・試聴・保存・根音変更・320 / 390pxでの6列を検査。最終的な文字折り返しのCSS調整後もbuildと320pxでの全ボタンの文字あふれ検査が成功し、画面を目視しました。

### 旋律のリズム・音の動きの改善（2026-09-17）

- 作業前の辞典のカテゴリ・6列表示を `78cca69` にコミットしました。
- 旋律generator version 2を追加。8拍のモチーフに長短・休符・前半と後半の応答を導入し、同音連打へのペナルティ、固定した中心音域、activityに応じた輪郭幅で単調さを減らしました。弱拍の短い発音から次の実音への半音接続は上下の近い側を選び、休符は保持します。
- C → A7 → Dm → G（各4拍）、密度0.55・activity0.35、SeedとmotifSeedを1〜32で比較。休符を挟まない隣接音の同音率は旧57.3%→新7.3%、1フレーズの異なるMIDI音高数は平均5.59→7.00、音価の種類数は平均1.00→2.53。7半音を超える跳躍は新0.19%。この特定条件の構造的な比較であり、聴感の優劣を保証する数値ではありません。
- 旧version 1の生成処理は維持。保存済み音符を読み込んだだけでは変えず、再生成または音域・密度・activity・Seed変更でversion 2を使用します。音量変更等では移行しません。設定画面に旧データ向けの案内を追加しました。
- `npm run check` が成功（78 unit testsを含む）。音域、強拍の和声音、全調、Seed再現、旧保存データ、再生成と伴奏保持、短いLive区間や浮動小数点誤差を含む時間境界を検査。PC／モバイルの全ブラウザテスト76件が成功し、Timeline・Live・予約上書き・停止・保存・旋律の連続生成を確認しました。
- 実機での試聴や旋律の自然さの聴感評価は未実施です。

### 最初の演奏操作で音声を開始（2026-09-17）

- コード選択、辞典の試聴・進行への追加、保存済み進行の再生、配置比較、五度圏の試聴・移動、連続生成を音声準備と接続。「音声を開始」を先に押す必要をなくしました。任意の手動準備ボタンは保持しています。
- ユーザー操作のハンドラーからAudioContextを開始し、初回の音源取得後にその操作を実行。準備処理を共有し、後続操作を重複実行しません。停止・編集・画面移動・非表示・音色変更で待機中の操作を取り消します。
- 辞典のEnter操作でも初回から発音し、Piano取得失敗時はElectric pianoで操作を完了。中断されたAudioContextは「再開」の操作で復帰します。
- `npm run check` 成功（78 unit testsを含む）。PC／モバイルの全ブラウザテスト88件が成功。新規12件では事前準備なしのコード発音・保存済み進行の再生・辞典のキーボード試聴、遅延読込中の停止・画面移動・編集、および音源取得失敗時の発音を検査しました。
- ページ表示だけで無操作の自動発音は行いません。Safari/iOS等の実機でのジェスチャー解除・中断復帰の手動検証は未実施です。

### 読み込み表示のレイアウト固定（2026-09-17）

- 音源の読み込み表示を独立した通知パネルから再生操作欄の既存ステータスへ移動。長い表示は一行で省略し、下のパネルを押し下げません。
- 読み込みを意図的に遅延させ、320 / 390 / 1280pxで調パネル・ダイアトニックパネルの上端位置が表示前と読み込み中で一致することをブラウザで確認。型検査・buildと、コミット前の `npm run check`（78 unit testsを含む）が成功しました。

## Harmonic Space（2026-09-17）

- 設定をヘッダー右上へ移動し、下部右端に独立した「コード探索」を追加。C majorのcore 14、near 15、outer 13、計42ノードを配置。今回の範囲と構造は `docs/HARMONIC_SPACE.md` に記載。
- `npm run check`: strict型検査、ESLint、Prettier、80 unit tests、production build。
- Playwright Chromium: 既存機能を含む50シナリオ × desktop/mobile = 100 testsが成功。設定への操作経路の変更を既存テストへ反映。
- `/chordscape-smoke/` で探索専用6シナリオ × desktop/mobile = 12 testsが成功。初回クリックの実際の音声出力、停止、画面移動・戻るによる読込待ちの取消、演奏進行・設定・調が不変であることを確認。
- 全ノード間の重なりなし、Tab/方向キー/Enter/Spaceによる操作、端のノードへのスクロール、320pxのページoverflowなしを検査。desktopの全体配置とmobileの画面画像も目視確認。
- 進行推薦、候補の発光、style切替、調切替、automatic voice leadingは未実装。人による聴感評価・実機Safari/iOS検証・公開は実施していない。
